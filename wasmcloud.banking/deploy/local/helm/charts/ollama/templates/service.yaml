---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  selector:
    name: {{ .Release.Name }}
  ports:
  - name: ollama-api
    port: 11434
    targetPort: 11434
    protocol: TCP